{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch BMI",
            "program": "${workspaceFolder}/bmi",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}